# Advisory Hooks

This directory contains advisory checklists and validation-oriented preflight helpers for AI and human operators.

They are not auto-executing hooks and should not mutate the repository. Any future auto-executing hook must be introduced through a reviewed PR that explains the command, blast radius, rollback path, and verification evidence.
